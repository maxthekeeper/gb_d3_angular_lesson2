(function (angular) {
    'use strict';

    angular
        .module('app')
        .controller('root.controller',
        ['$rootScope', '$scope',
            function($rootScope, $scope) {

            }
        ]);

})(angular);