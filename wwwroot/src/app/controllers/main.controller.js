(function (angular) {
    'use strict';

    angular
        .module('app')
        .controller('main.controller',
        ['$rootScope', '$scope',
            function($rootScope, $scope) {

            }
        ]);

})(angular);